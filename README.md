# GODO
Todo application created in go, to learn backend development

## Setup
To run the application Docker is recommended as we'll be using docker container for database setup.
In docker compose file contains env variables for database but in future they'll be moved to .env file